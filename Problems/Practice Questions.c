#include <stdio.h>
int main(void)
{
	int height, width, parimeter;
	printf("Input the height of the Rectangle:");
	scanf("%d", &height);
	printf("Input the width of the Rectangle:");
    scanf("%d", &width);
    parameter =(height+width)*2;
    printf("The parimeter is %d", parimeter);
    return 0;
}
