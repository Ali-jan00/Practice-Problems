#include <stdio.h>
int main ()
{
	char vowels;
	int count = 0;
	printf("Enter any string : ");
	scanf("%c",vowels);
	for(int i;i <= 10; i++ ){
		if(vowels == a || vowels == b)
		count++;
	}
	return 0;
}